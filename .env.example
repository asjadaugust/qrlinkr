# Database Configuration
POSTGRES_DB=qrlinkr
POSTGRES_USER=qrlinkr_user
POSTGRES_PASSWORD=changeme_in_production
DATABASE_URL=postgresql://qrlinkr_user:changeme_in_production@postgres:5432/qrlinkr

# Backend Configuration
PORT=3001
HOST=0.0.0.0

# Frontend Configuration
# The URL of the backend API that the frontend will use
# In development with Docker Compose, this is http://backend:3001
# For browser access, this should be the publicly accessible backend URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001

# Auth Configuration (Uncomment and configure when adding authentication)
# AUTH_SECRET=your_auth_secret_key
# CLERK_SECRET_KEY=your_clerk_secret_key
# CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
